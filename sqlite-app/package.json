{
  "name": "sqlite-project",
  "version": "1.0.0",
  "main": "server.ts",
  "scripts": {
    "dev_": "node --permission --allow-fs-read=server.ts --watch server.ts",
    "dev": "node --watch server.ts"
  },
  "keywords": ["node", "sqlite"],
  "author": "Xavier Brinon",
  "license": "ISC",
  "type": "module",
  "description": "Build an app around sqlite using node. Based on the FEM course on Sqlite.",
  "dependencies": {
    "@fastify/static": "^7.0.4",
    "fastify": "^4.28.1",
    "handlebars": "^4.7.8",
    "htmx.org": "^1.9.12"
  }
}
